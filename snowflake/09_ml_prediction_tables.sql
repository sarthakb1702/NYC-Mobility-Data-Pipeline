USE DATABASE NYC_MOBILITY;

CREATE OR REPLACE TABLE NYC_MOBILITY.MARTS.ML_ZONE_DEMAND_PREDICTIONS (
    DATE_KEY NUMBER,
    LOCATION_KEY NUMBER,
    FULL_DATE DATE,
    ACTUAL_TRIPS FLOAT,
    PREDICTED_TRIPS FLOAT
);

CREATE OR REPLACE TABLE NYC_MOBILITY.MARTS.ML_TRIP_DURATION_PREDICTIONS (
    TRIP_KEY VARCHAR,
    TAXI_TYPE VARCHAR,
    DATE_KEY NUMBER,
    PICKUP_LOCATION_KEY NUMBER,
    DROPOFF_LOCATION_KEY NUMBER,
    PICKUP_DATETIME TIMESTAMP_NTZ,
    TRIP_DISTANCE FLOAT,
    ACTUAL_DURATION_MINUTES FLOAT,
    PREDICTED_DURATION_MINUTES FLOAT
);